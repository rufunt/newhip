<div class="container">
	<h1>Hiptime</h1>

	<% @items.each do |item| %>
		<h3><%= link_to item.title, item %></h3>
		<p><%= item.description %></p>
	<% end %>

	<%= link_to "New Item", new_item_path %>
</div>
